<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Display name can be adjusted per target in Xcode (Info tab) -->
    <key>CFBundleDisplayName</key>
    <string>Taxi Driver</string>

    <!-- Merge common keys from Info.plist via build setting INFOPLIST_OTHER_PREPROCESSOR_FLAGS if desired.
         Alternatively, copy the content of Info.plist here and adjust differences. For brevity, only the driver-specific
         background modes are listed. In Xcode, set this file as INFOPLIST_FILE for the Driver target/scheme. -->

    <!-- Background location capability for driver app -->
    <key>UIBackgroundModes</key>
    <array>
        <string>location</string>
    </array>

    <key>NSLocationWhenInUseUsageDescription</key>
    <string>We use your location to find and navigate to riders.</string>
    <key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
    <string>We need background location to update your position while driving.</string>
    <key>NSLocationAlwaysUsageDescription</key>
    <string>Background location helps us assign rides and estimate ETAs.</string>

    <!-- Required minimum keys to satisfy iOS build; the Flutter tool will merge more at build time. -->
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <true/>
    </dict>
    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>
    <key>UIMainStoryboardFile</key>
    <string>Main</string>
</dict>
</plist>
