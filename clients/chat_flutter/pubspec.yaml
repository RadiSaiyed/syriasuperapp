name: chat_flutter
description: Minimal Chat client (E2E-ready, ciphertext only on server)
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.2
  web_socket_channel: ^2.4.0
  uuid: ^4.5.1
  cryptography: ^2.5.0
  shared_ui:
    path: ../shared_ui
  file_selector: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
